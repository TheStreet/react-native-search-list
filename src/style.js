import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    noticeContainer: {
        justifyContent: 'center'
    },
    noticeText: {
        padding: 10,
        textAlign: 'center',
        color: 'grey',
        fontWeight: '400',
        fontSize: 14
    }
});